{
    "name": "deli-app",
    "private": true,
    "version": "1.0.0",
    "type": "module",
    "scripts": {
      "start": "cd ./frontend && npm install && npm run build && cd ../backend && npm install && npm run build && npm start"
    }
}
  